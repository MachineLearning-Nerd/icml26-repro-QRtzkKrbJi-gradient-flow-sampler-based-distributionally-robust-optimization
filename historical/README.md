# Historical rejected baseline

`judged_space_735012f52396955c5734e8fc568adfdf2abda757/` is the exact judged
Hugging Face Space revision from the previous 4/12 result. Its files are
preserved byte-for-byte and validated by `manifest.sha256`.

The old page titled “Verification run” is historical rejected evidence. It is
retained for provenance and must never be presented as the current verifier.

